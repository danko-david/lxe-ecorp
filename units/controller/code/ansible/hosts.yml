all:
  hosts:
    localhost:
  vars:
    ansible_connection: local
    DNS_CORE_ZONE_NAME: "{{ lookup('env', 'DNS_CORE_ZONE_NAME') }}"

    ad_exec: "docker exec -t ecorp-dc-1"
    gitlab_url: "http://gitlab.{{ DNS_CORE_ZONE_NAME }}"